# TODO: Fix TypeScript Build Errors

- [ ] Update `tsconfig.app.json` to include "node" in the `types` array
- [ ] Change `src={null}` to `src={undefined}` in `src/components/ekskul.tsx` line 382
- [ ] Run `npm run build` to verify the fixes resolve the errors
